---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04
  - name: ubuntu-16.04

suites:
  - name: default
    attributes:
      ruby:
        version: '2.3'
    run_list:
      - recipe[apt::default]
      - recipe[bcs_ruby::default]
